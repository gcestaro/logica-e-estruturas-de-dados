function atribuicao() {

    let a = 1

}


function aritmeticos(a, b) {

    let soma = a + b
    
    let subtracao = a - b
    
    let multiplicacao = a * b
    
    let divisao = a / b

    let mod = a % b

    let incremento = a++

    let decremento = a--

    // em algumas linguagens existe exponenciacao como aritmetico com simbolos como ^ ou **. Em javascript não temos :( mas podemos usar: Math.pow(numero, exponente)

}

function relacionais(a, b){


    let maiorQue = a > b

    let maiorOuIgualA = a >= b

    let menorQue = a < b

    let menorOuIgualA = a <= b

    let igualA = a == b // em javascript também existe ===

    let diferenteDe = a != b // em javascript também existe !==

}

function logicos(a, b) {

    let e = a && b

    let ou = a || b

    let nao = !a

    // em algumas linguagens também podemos usar outros operadores lógicos, como o XOU (Em ingles, XOR)
}